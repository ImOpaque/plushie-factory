/** Game simulation runs at this many ticks per second (each tick accrues passive income for 1/TICKS_PER_SECOND of a second). */
export const TICKS_PER_SECOND = 10;

export const TICK_INTERVAL_MS = 1000 / TICKS_PER_SECOND;
